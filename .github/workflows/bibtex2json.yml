name: Convert .bib to .json

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - name: Read .bib and write .json
        uses: docker://pandoc/core:2.14.1
        with:
          args: >-
            --from=bibtex
            --to=csljson
            --output=content/publication_resources/json/lahti.json
            content/publication_resources/bibtex/lahti_edit.bib
      - name: Commit results
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add ./content/publication_resources/json/
          git commit content/publication_resources/json/lahti.json -m 'bibtex2json update' || echo "No changes to commit"
          git push origin || echo "No changes to commit"
